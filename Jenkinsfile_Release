pipeline {
    agent any
    stages {
        stage('Build') { steps { bat 'npm install && npm run build' } }
        stage('Archive') { 
            steps { 
                // Archive the build folder (dist or build)
                archiveArtifacts artifacts: 'dist/**/*', fingerprint: true 
            } 
        }
    }
}
